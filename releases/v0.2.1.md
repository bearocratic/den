# v0.2.1

_Security maintenance — clears the two open advisories on Den's dependency tree._

## Fixed

- Bumped `git2` 0.19.0 → 0.20.4, clearing **GHSA-j39j-6gw9-jw6h** / RUSTSEC-2026-0008 (potential undefined behaviour when dereferencing a `Buf` struct).
- Bumped `ratatui` 0.28 → 0.30, which pulls `lru` 0.12.5 → 0.16.4 and clears **GHSA-rhfx-m35p-ff5j** (`IterMut` Stacked Borrows violation).

Neither advisory was reachable from Den's runtime paths in practice, but both are now closed out.

## Install

```sh
brew tap bearocratic/tap
brew install den
# or upgrade
brew upgrade den
```
