{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the panel may ask of the app: its own commands, and the file dialog behind 'Add folder'.",
  "windows": ["panel"],
  "permissions": ["core:default", "dialog:allow-open"]
}
